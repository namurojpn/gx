export interface Project {
  slug: string;
  name: string;
  shortName: string;
  cardImageWide: string;
  cardImage: string;
  category: string;
}

export interface NewsItem {
  date: string;
  text: string;
}
